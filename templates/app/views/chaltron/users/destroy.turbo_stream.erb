<% if current_user != @user %>
  <%= turbo_stream.update 'chaltron_users' do %>
    <%= render partial: 'users', locals: { users: @users, pagy: @pagy, count_filters: @count_filters } %>
  <% end %>
<% end %>

<%= render_turbo_stream_flash_messages %>