<nav id='navbar-top' class='navbar navbar-expand-lg navbar-dark bg-dark'>
  <div class='container-fluid mx-auto'>
    <%= link_to 'Home page', root_path, class: 'navbar-brand' %>
    <button class='navbar-toggler' type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent'
            aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation'>
      <span class='navbar-toggler-icon'></span>
    </button>
    <div class='collapse navbar-collapse' id='navbarSupportedContent'>
      <ul class='navbar-nav me-auto'>
        <li class='nav-item dropdown'>

          <%
            urls = [greppa_requests_path, new_greppa_request_path]
            klass = class_names('nav-link', 'dropdown-toggle', { active: urls.any? { |url| current_page?(url) } })
          %>
          <%= link_to '#', id: 'navbarDropdown', class: klass, data: { 'bs-toggle' => 'dropdown' } do %>
            <%= icon :fas, :search, 'Greppa' %>
          <% end %>
          <ul class='dropdown-menu dropdown-menu-dark' aria-labelledby='navbarDropdown'>
            <li>
              <%= link_to new_greppa_request_path,
                          class: class_names('dropdown-item', { active: current_page?(new_greppa_request_path) }) do %>
                <%= icon :fas, 'search-plus', 'Nuova ricerca' %>
              <% end %>
            </li>
            <li>
              <%= link_to greppa_requests_path,
                          class: class_names('dropdown-item', { active: current_page?(greppa_requests_path) }) do %>
                <%= icon :fas, :history, 'Ricerche effettuate' %>
              <% end %>
            </li>
          </ul>
        </li>
      </ul>
      <%# Right navbar %>
      <%= render 'chaltron/admin_navbar' %>
      <%= render 'chaltron/login_navbar' %>
    </div>
  </div>
</nav>
